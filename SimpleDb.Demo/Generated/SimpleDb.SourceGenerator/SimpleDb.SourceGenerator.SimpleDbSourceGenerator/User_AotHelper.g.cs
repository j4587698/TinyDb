// <auto-generated/>
using System;
using SimpleDb.Bson;
// DEBUG: Namespace = 'SimpleDb.Demo'
using SimpleDb.Demo;

namespace SimpleDb.Demo
{
    /// <summary>
    /// User 的AOT支持帮助器（源代码生成器生成）
    /// </summary>
    public static class UserAotHelper
    {

        /// <summary>
        /// 获取实体的ID值（AOT兼容）
        /// </summary>
        /// <param name="entity">实体实例</param>
        /// <returns>ID值</returns>
        public static BsonValue GetId(User entity)
        {
            if (entity == null) return BsonNull.Value;
            // 硬编码ID属性访问 - 避免AOT反射问题
            return new BsonObjectId(entity.Id);
        }

        /// <summary>
        /// 设置实体的ID值（AOT兼容）
        /// </summary>
        /// <param name="entity">实体实例</param>
        /// <param name="id">ID值</param>
        public static void SetId(User entity, BsonValue id)
        {
            if (entity == null || id?.IsNull != false) return;
            if (id is BsonObjectId bsonObjectId)
                entity.Id = bsonObjectId.Value;
        }

        /// <summary>
        /// 检查实体是否有有效的ID（AOT兼容）
        /// </summary>
        /// <param name="entity">实体实例</param>
        /// <returns>是否有有效ID</returns>
        public static bool HasValidId(User entity)
        {
            if (entity == null) return false;
            // 硬编码ID属性验证 - 避免AOT反射问题
            return entity.Id != ObjectId.Empty;
        }

        /// <summary>
        /// 为实体类生成实例方法包装器
        /// </summary>
        public static void PopulateInstanceMethods(User entity)
        {
            // 这个方法用于在运行时为实体添加实例方法（如果需要）
        }
    }
}
